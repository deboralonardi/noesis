# assets

Drop the final landing-page hero image here as `hero-image.jpg` (or update
the `src` in `js/views/landing.js` if you use a different filename/format).

Recommended size: 380 × 480 (matches the hero frame in the approved design).
Until the file exists, the landing page shows a dashed placeholder box
automatically — no code changes needed once the image is added.
